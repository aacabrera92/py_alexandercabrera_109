

for mes in range(4, 13):

    inicio = datetime.date(2024, mes, 1)
    fin = datetime.date(2024, mes, 30)
    delta = (fin - inicio).days
    #return inicio + datetime.timedelta(days=random.randint(0, delta))


    # Calcular dias del mes
    '''

        inicio = datetime.date(2024, mes, 1)
    if mes == 12:
        fin = datetime.date(2024, 12, 31)
    else:
        fin = datetime.date(2024, mes + 1, 1) - datetime.timedelta
    
    delta = (fin - inicio).days
    '''